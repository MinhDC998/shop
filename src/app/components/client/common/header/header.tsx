import React, { FC } from 'react';
import '@client/common/header/header.scss';

const ClientHeader: FC = () => {
    return (
        <div id="client-header" className="mt-24">
            header
        </div>
    );
};

export default ClientHeader;
